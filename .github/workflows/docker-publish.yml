name: Docker

on:
  push:
    branches: [main, 'homelab/**']
    tags: ['v*']

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          # -patched, not -backend: a hand `docker push` to
          # ghcr.io/<owner>/homelable-backend created that package outside any
          # repo, and a repo's GITHUB_TOKEN cannot write a package it does not
          # own. Granting that access is UI-only, so this publishes under a name
          # CI creates and therefore owns. Rename back once the orphan is gone.
          - image: ghcr.io/${{ github.repository_owner }}/homelable-backend-patched
            context: .
            dockerfile: Dockerfile.backend
            build_args: ""
          - image: ghcr.io/${{ github.repository_owner }}/homelable-frontend
            context: .
            dockerfile: Dockerfile.frontend
            build_args: ""
          - image: ghcr.io/${{ github.repository_owner }}/homelable-frontend-standalone
            context: .
            dockerfile: Dockerfile.frontend
            build_args: "VITE_STANDALONE=true"
          - image: ghcr.io/${{ github.repository_owner }}/homelable-mcp
            context: ./mcp
            dockerfile: Dockerfile.mcp
            build_args: ""

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ matrix.image }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.context }}/${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: ${{ matrix.build_args }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
